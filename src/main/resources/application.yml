spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cards_ms_db
    password: root123
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL81Dialect



server:
  port: 8092